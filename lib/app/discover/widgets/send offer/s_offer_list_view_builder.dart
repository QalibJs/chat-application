import 'package:flutter/material.dart';

class SendOfferListViewWidget extends StatelessWidget {
  const SendOfferListViewWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}